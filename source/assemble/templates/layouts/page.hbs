{{>html-header}}
	{{>site-nav}}
	<div class="page-wrapper">
		<section class="section-main">
			<!--h2>{{this.page.title}}</h2-->
			{{>body}}
		</section>
		{{>site-sidebar}}
	</div>
{{>html-footer}}